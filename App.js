const heading = React.createElement(
  "h1",
  { id: "heading" },
  "Hello World from React! My first React program."
);
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(heading);
